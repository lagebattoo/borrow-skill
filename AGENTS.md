# 借物 Skill 项目约定

本项目用于设计「借物 · Borrow」Skill。开始设计、实现、审查或修改该 Skill 前，必须先阅读 [统一设计规格 v1](./借物Skill统一设计规格v1.md)，再按需查阅 [原始指导方案与定位图](./借物Skill指导方案.md)。

## 命名与设计依据

- GitHub 仓库名称：`borrow-skill`；Skill 目录及标识：`borrow`；展示名称：`借物 · Borrow`。
- 统一设计规格 v1 是当前主要设计依据；旧指南保留原始要求和定位背景，不删除。
- 当前用户明确指令优先；新旧文档对同一事项存在明确差异时，以 v1 为准。仅旧指南涉及的要求继续保留；遇到无法判断的冲突应明确指出，不自行改写用户目标。
- v1 正文是用户原始规格；末尾附录单独记录已确认命名、建议文件结构和待完善事项。待完善事项不视为已确认的新规则。

## 当前阶段

发布版本为 `v0.1.0` 早期版本，采用 MIT 许可证。面向使用者的安装、调用和验证范围说明在 [README.md](./README.md)，版本说明在 [releases/v0.1.0.md](./releases/v0.1.0.md)。原始规格 v1 与发布版本号分别维护，不因版本发布改写用户原始正文。

当前已固化原始要求、统一设计规格 v1 和命名，并编写 [borrow/SKILL.md](./borrow/SKILL.md)、参考规则、模板和展示配置。格式、引用与原始正文保留检查已通过；8 个离线行为场景（9 次独立 CLI 调用）已通过，证据和限制见 [验证报告](./tests/evals/REPORT.md)。本轮未改动 Skill 规则，尚未安装到日常环境或验证实时外部选型。规格中的流程、搜索和示例是 Skill 的设计要求，不代表读取文档时就要执行选型、安装依赖或开展业务编码。

## 防止定位偏移

- 「借物」是软件方案设计阶段的 Reuse Planning Skill；先理解需求和约束，再分析复用。
- 主要定位链路是：借物 → 制定 Reuse Policy → 项目 AGENTS.md → Codex / Agent 的 search-first、coding、debugging、refactoring。
- 长期复用政策与用户选择的长期 Reuse Budget 进入目标项目的 AGENTS.md；临时候选、版本、搜索结果和详细决策进入 Plan 或 REUSE_PLAN.md。
- Reuse Budget 未指定时为 Balanced；Maximum Reuse 必须由用户主动选择。复用以降低 Total Engineering Ownership 为目标，不以依赖数量或少写代码为目标。
- 不得擅自扩展为通用 Coding Agent、搜索器、自动依赖安装器或 search-first 替代品。需要改变定位时，先向用户明确提出变更。

## 修改要求

指导方案和统一设计规格 v1 保留用户提供的完整原始正文，补充说明放在各自末尾。后续整理或实现不得静默删减、弱化这些要求；附加建议应明确标识，避免混入用户原始要求。维护规则时以 v1 为主要入口，避免在多份文档中复制和分别修改同一套规则。
